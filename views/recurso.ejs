<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Recurso</title> <!--nombre del instituto y edificio-->
        
        <link rel="stylesheet" href="/resourses/css/reset.css">
        <link rel="stylesheet" href="/resourses/css/style-pagina.css">

        <link rel="preconnect" href="https://fonts.googleapis.com"> <!--tipo de letra-->
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    </head>

    <body>
        <header>
            <div class="navegacion">
                <h1> <img src=""></h1> <!--logo-->
                <nav> <!--panel de navegación-->
                    <ul> <!--lista horizontal, para la navegación-->
                        <%if(cookie.rol == "Administrador") {%>
                            <li><a href="/usuario">Usuarios</a></li>
                            <li><a href="/movimiento">Movimientos</a></li>
                        <% } %>
                        <li><a href="/instituto">Institutos</a></li> <!-- <a> hace referencia que hay un vinculo-->
                        <li><a href="/salir">Salir</a></li>
                    </ul>
                </nav>
            </div>
        </header>

        <main>
            <div class="titulos">
                <button class="botonIcono atras" id="atras"> 
                    <a href="/salon">
                        <img src="/resourses/image/flecha-hacia-atras.png" alt="Atras">
                    </a>    
               </button>
                <h1> <%= cookie.nombreSalon %></h1> <!--poner el nombre del instituto-->
            </div>
            
            <div> <!--titulo, subtitulo y formulario-->

                <section class="tablaBody">
                    <div class="tabla">
                        <div class="tablaEncabezado">
                            <div>
                                <h1 class="subtitulos">RECURSOS</h1>
                                <% if(cookie.rol != "Lector"){ %>
                                <button class="botonIcono" onclick="add()">
                                    <img src="/resourses/image/agregar.png" alt="Agregar edificio">
                                </button>
                                <% } %>
                            </div>
                            <!--
                            <div class="buscar">
                                <select id="buscarPor" class="buscarPor"> lista para seleccionar-->
                                    <!--opciones-->
                                    <!--<option selected >Nombre</option>
                                    <option>Posición</option> 
                                    <option>Serial</option>
                                    <option>Marca</option>
                                    <option>Color</option>
                                    <option>Precio</option>
                                    <option>Estado</option>
                                    <option>Descripción</option>
                                    <option>Acciones</option>
                                </select>
                                <input class="inputBuscar" placeholder="Buscar"/>
                                <button class="botonIcono">
                                    <img src="/resourses/image/buscar.png" alt="">
                                </button>
                            </div>-->
                        </div>
                    </div>    
                    <% if(results.length > 0) { %>
                        <div class="tablaSection">
                            <table>
                                <thead>
                                    <tr>
                                        <th>Nombre</th>
                                        <th>Posición</th>
                                        <th>Serial</th>
                                        <th>Color</th>                                     
                                        <th>Estado</th>                                        
                                        <th>Acciones</th>
                                    </tr>
                                </thead> 
                        <% results.forEach(recurso => { %>
                            
                            <tbody id="tbodyRecursos"> <!--id para rellenar la tabla-->
                                            <td><%= recurso.nombre %></td>
                                            <td><%= recurso.posicion %></td>
                                            <td><%= recurso.serial %></td>
                                            <td><%= recurso.color %></td>
                                            <td><%= recurso.estado %></td>
                                            <td >
                                                <% if(cookie.rol != "Lector"){ %>
                                                <div class="contenedorAcciones">
                                                    <button class="botonIcono icono" id="editarIcono" onclick="edit('<%= recurso.id_recurso %>', '<%= recurso.nombre %>', '<%= recurso.color %>', '<%= recurso.marca %>', '<%= recurso.estado %>', '<%= recurso.precio %>', '<%= recurso.serial %>', '<%= recurso.posicion %>', '<%= recurso.descripcion %>')">
                                                        <img src="/resourses/image/editar.png">
                                                    </button>
                                                    <button class="botonIcono icono" id="borrarIcono">    
                                                        <img src="/resourses/image/borrar.png" onclick="confirm('<%=recurso.id_recurso%>',
                                                        '<%= recurso.nombre%>')">
                                                    </button>
                                                </div>
                                                <% } %>
                                            </td>
                             </tbody>
                        <%  }); %>
                                
                            </table>
                        </div>
                        <% } else { %>
                            <div >
                                <h2 class="subtitulos">Antes debe de crear recursos</h2>
                            </div>
                        <% } %>
                    </div>
                </section>

                <!--MOSTRAR LOS EDIFICIOS DISPONIBLES-->
        </main>
        
        <footer> <!--Etiqueta de pie de página-->
            <p class="Copyright"> &copy Copyright  2024 </p>
            <p class="Copyright"> DERECHOS RESERVADOS </p>
            <p class="Copyright"> LOS PANZONES :D</p>

        </footer>

        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.10.8/dist/sweetalert2.all.min.js"></script>
        <script src="/resourses/scripts/recurso_app.js"></script>

    </body>
</html>